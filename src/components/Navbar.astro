---
import config from '@utils/config';
import { t, localeFlags, localeNames } from '@utils/i18n';

interface Props {
  locale: string;
  localeData: any;
}

const { locale, localeData } = Astro.props;
const availableLocales = config.locale.available;

const navLinks = [
  { href: '/', key: 'nav.home' },
  { href: '/shop', key: 'nav.shop' },
  { href: '/rules', key: 'nav.rules' },
  { href: '/about', key: 'nav.about' },
];

const currentPath = Astro.url.pathname;
---

<nav class="fixed top-0 left-0 right-0 z-50">
  <div class="mx-auto max-w-6xl px-5">
    <div class="mt-4 rounded-sm bg-white/[0.03] backdrop-blur-xl border border-white/[0.06]">
      <div class="flex items-center justify-between h-14 px-4">
        
        <!-- Logo -->
        <a href="/" class="flex items-center gap-2.5 group">
          <div class="w-8 h-8 rounded-sm bg-[#F7EE13] flex items-center justify-center text-sm">
            ⚔
          </div>
          <span class="font-pixel text-sm text-white/90 group-hover:text-white transition-colors tracking-wide">
            {config.server.name}
          </span>
        </a>
        
        <!-- Desktop Nav -->
        <div class="hidden md:flex items-center gap-0.5">
          {navLinks.map(link => (
            <a
              href={link.href}
              class={`px-3.5 py-1.5 rounded-sm font-pixel text-[11px] transition-all duration-200 ${
                currentPath === link.href
                  ? 'text-white bg-white/[0.08]'
                  : 'text-white/40 hover:text-white/70 hover:bg-white/[0.04]'
              }`}
            >
              {t(localeData, link.key)}
            </a>
          ))}
        </div>
        
        <!-- Right Side -->
        <div class="flex items-center gap-2">
          <!-- Language -->
          <div class="relative group/lang" id="lang-menu">
            <button
              type="button"
              id="lang-trigger"
              aria-haspopup="true"
              aria-expanded="false"
              class="flex items-center gap-2 px-2.5 py-1.5 rounded-sm hover:bg-white/[0.04] transition-all"
            >
              <img
                src={localeFlags[locale] || localeFlags['en']}
                alt={locale}
                class="w-4 h-4 rounded-full object-cover"
                onerror="this.style.display='none'"
              />
              <span class="font-pixel text-[10px] text-white/50 hidden sm:inline">{localeNames[locale] || locale}</span>
              <svg class="w-3 h-3 text-white/30" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </button>
            
            <div id="lang-dropdown" class="absolute right-0 top-full mt-2 w-40 bg-[#0A0A0A] border border-white/[0.06] rounded-sm shadow-2xl opacity-0 invisible group-hover/lang:opacity-100 group-hover/lang:visible transition-all duration-200 overflow-hidden z-50">
              {availableLocales.map(lang => {
                const switchUrl = new URL(Astro.url);
                switchUrl.searchParams.set('lang', lang);
                return (
                  <a
                    href={switchUrl.pathname + switchUrl.search}
                    data-lang={lang}
                    class={`lang-option flex items-center gap-3 px-4 py-2.5 font-pixel text-[10px] transition-all ${
                      locale === lang
                        ? 'bg-white/[0.06] text-[#F7EE13]'
                        : 'text-white/50 hover:bg-white/[0.04] hover:text-white/80'
                    }`}
                  >
                    <img
                      src={localeFlags[lang] || localeFlags['en']}
                      alt={lang}
                      class="w-4 h-4 rounded-full object-cover"
                      onerror="this.style.display='none'"
                    />
                    {localeNames[lang] || lang}
                  </a>
                );
              })}
            </div>
          </div>
          
          <!-- Discord -->
          <a
            href={config.server.discordUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="flex items-center gap-1.5 px-3 py-1.5 rounded-sm bg-[#5865F2]/10 border border-[#5865F2]/20 hover:bg-[#5865F2]/20 transition-all"
          >
            <svg class="w-3.5 h-3.5 text-[#5865F2]" viewBox="0 0 24 24" fill="currentColor">
              <path d="M20.317 4.3698a19.7913 19.7913 0 00-4.8851-1.5152.0741.0741 0 00-.0785.0371c-.211.3753-.4447.8648-.6083 1.2495-1.8447-.2762-3.68-.2762-5.4868 0-.1636-.3933-.4058-.8742-.6177-1.2495a.077.077 0 00-.0785-.037 19.7363 19.7363 0 00-4.8852 1.515.0699.0699 0 00-.0321.0277C.5334 9.0458-.319 13.5799.0992 18.0578a.0824.0824 0 00.0312.0561c2.0528 1.5076 4.0413 2.4228 5.9929 3.0294a.0777.0777 0 00.0842-.0276c.4616-.6304.8731-1.2952 1.226-1.9942a.076.076 0 00-.0416-.1057c-.6528-.2476-1.2743-.5495-1.8722-.8923a.077.077 0 01-.0076-.1277c.1258-.0943.2517-.1923.3718-.2914a.0743.0743 0 01.0776-.0105c3.9278 1.7933 8.18 1.7933 12.0614 0a.0739.0739 0 01.0785.0095c.1202.099.246.1981.3728.2924a.077.077 0 01-.0066.1276 12.2986 12.2986 0 01-1.873.8914.0766.0766 0 00-.0407.1067c.3604.698.7719 1.3628 1.225 1.9932a.076.076 0 00.0842.0286c1.961-.6067 3.9495-1.5219 6.0023-3.0294a.077.077 0 00.0313-.0552c.5004-5.177-.8382-9.6739-3.5485-13.6604a.061.061 0 00-.0312-.0286zM8.02 15.3312c-1.1825 0-2.1569-1.0857-2.1569-2.419 0-1.3332.9555-2.4189 2.157-2.4189 1.2108 0 2.1757 1.0952 2.1568 2.419 0 1.3332-.9555 2.4189-2.1569 2.4189zm7.9748 0c-1.1825 0-2.1569-1.0857-2.1569-2.419 0-1.3332.9554-2.4189 2.1569-2.4189 1.2108 0 2.1757 1.0952 2.1568 2.419 0 1.3332-.946 2.4189-2.1568 2.4189Z"/>
            </svg>
            <span class="font-pixel text-[10px] text-white/60 hidden sm:inline">{t(localeData, 'nav.discord')}</span>
          </a>
          
          <!-- Mobile toggle -->
          <button
            type="button"
            id="mobile-toggle"
            class="md:hidden p-2 rounded-sm hover:bg-white/[0.04] transition-all"
          >
            <svg class="w-4 h-4 text-white/60" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </div>
      
      <!-- Mobile menu -->
      <div id="mobile-menu" class="hidden md:hidden border-t border-white/[0.06] px-4 pb-3">
        <div class="flex flex-col gap-0.5 pt-2">
          {navLinks.map(link => (
            <a
              href={link.href}
              class={`px-3 py-2.5 rounded-sm font-pixel text-xs transition-all ${
                currentPath === link.href
                  ? 'text-white bg-white/[0.06]'
                  : 'text-white/40 hover:text-white/70 hover:bg-white/[0.03]'
              }`}
            >
              {t(localeData, link.key)}
            </a>
          ))}
        </div>

        <div class="mt-3 border-t border-white/[0.06] pt-3">
          <div class="px-3 pb-2 font-pixel text-[10px] text-white/30 uppercase tracking-[0.15em]">Language</div>
          <div class="flex flex-col gap-0.5">
            {availableLocales.map(lang => {
              const switchUrl = new URL(Astro.url);
              switchUrl.searchParams.set('lang', lang);
              return (
                <a
                  href={switchUrl.pathname + switchUrl.search}
                  data-lang={lang}
                  class={`lang-option px-3 py-2.5 rounded-sm font-pixel text-xs transition-all ${
                    locale === lang
                      ? 'text-[#F7EE13] bg-white/[0.06]'
                      : 'text-white/40 hover:text-white/70 hover:bg-white/[0.03]'
                  }`}
                >
                  {localeNames[lang] || lang}
                </a>
              );
            })}
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>

<script>
  const toggle = document.getElementById('mobile-toggle');
  const menu = document.getElementById('mobile-menu');
  toggle?.addEventListener('click', () => menu?.classList.toggle('hidden'));

  const langMenu = document.getElementById('lang-menu');
  const langTrigger = document.getElementById('lang-trigger');
  const langDropdown = document.getElementById('lang-dropdown');
  const langOptions = document.querySelectorAll('.lang-option');

  const setLangOpen = (open) => {
    if (!langTrigger || !langDropdown) return;
    langTrigger.setAttribute('aria-expanded', open ? 'true' : 'false');
    langDropdown.classList.toggle('opacity-100', open);
    langDropdown.classList.toggle('visible', open);
    langDropdown.classList.toggle('opacity-0', !open);
    langDropdown.classList.toggle('invisible', !open);
  };

  langTrigger?.addEventListener('click', (event) => {
    event.preventDefault();
    event.stopPropagation();
    const isOpen = langTrigger.getAttribute('aria-expanded') === 'true';
    setLangOpen(!isOpen);
  });

  document.addEventListener('click', (event) => {
    if (!langMenu?.contains(event.target)) {
      setLangOpen(false);
    }
  });

  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') {
      setLangOpen(false);
    }
  });

  langOptions.forEach((option) => {
    option.addEventListener('click', () => {
      const nextLang = option.getAttribute('data-lang');
      if (nextLang) {
        document.cookie = `lang=${nextLang};path=/;max-age=31536000;SameSite=Lax`;
      }
      setLangOpen(false);
    });
  });
</script>
